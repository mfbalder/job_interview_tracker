<p style="color: green"><%= notice %></p>

<%= render @job %>

<div>
  <%= link_to "Edit this job", edit_job_path(@job) %> |
  <%= link_to "Back to jobs", jobs_path %> |
  <%= link_to "Add interview", new_interview_path(job_id: @job.id) %>

  <%= button_to "Destroy this job", @job, method: :delete %>
</div>
